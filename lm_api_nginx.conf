# Nginx 配置 - LM Studio API 反向代理
# 用于 HTTPS 支持和增强安全性
# 安装: sudo cp lm_api_nginx.conf /etc/nginx/sites-available/lm-api
#       sudo ln -s /etc/nginx/sites-available/lm-api /etc/nginx/sites-enabled/
#       sudo nginx -t && sudo systemctl reload nginx

server {
    listen 80;
    server_name lm.ham.vlsc.net;  # 修改为你的域名

    # 可选：重定向到 HTTPS
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8888;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 长连接设置（用于流式响应）
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;

        # 流式响应设置
        proxy_buffering off;
        proxy_cache off;
        proxy_set_header Connection '';
        chunked_transfer_encoding on;
    }
}

# HTTPS 配置（需要 SSL 证书）
# server {
#     listen 443 ssl http2;
#     server_name lm.ham.vlsc.net;
#
#     ssl_certificate /path/to/fullchain.pem;
#     ssl_certificate_key /path/to/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#
#     location / {
#         proxy_pass http://127.0.0.1:8888;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         proxy_buffering off;
#     }
# }
